# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE (keep local; don't force team prefs)
.idea/
*.swp
*.swo

# Scratch
*.local.md
_upstream_docs/
.env
.env.*

# claude code local data (plans, session cache, etc.)
.claude/

# audit script runtime state file - per-user, per-machine, not for committing
.skill-audit.json
